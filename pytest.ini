[pytest]
testpaths = tests
addopts = -q
# `python -m pytest` already puts the working directory on sys.path, this makes
# a bare `pytest` work too (the tests import custom_components.*).
pythonpath = .
